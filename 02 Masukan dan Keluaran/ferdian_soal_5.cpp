#include <iostream>
using namespace std;
int main() {
    string baris1, baris2, baris3, baris4, baris5;

    getline(cin, baris1);
    getline(cin, baris2);
    getline(cin, baris3);
    getline(cin, baris4);
    getline(cin, baris5);

    cout << baris1 << endl;
    cout << baris2 << endl;
    cout << baris3 << endl;
    cout << baris4 << endl;
    cout << baris5 << endl;

    return 0;
}